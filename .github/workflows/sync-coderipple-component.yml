name: CodeRipple Component Sync

on:
  workflow_dispatch:

jobs:
  api-test:
    runs-on: ubuntu-latest

    steps:
      - name: Test CodeRipple API
        run: |
          echo "CodeRipple automation workflow is running."
          curl -f https://getcoderipple.com/wp-json/
          echo "CodeRipple API connection successful."
